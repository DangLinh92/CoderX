extends ../menu

block content
    h3 Add new user
    .alert.alert-danger(role='alert')=error_text
    form(action="/users/new", method="post")
        input(type='text', placeholder='enter the name',name="name",value=userName ? userName :'')
        button(type="submit") Submit